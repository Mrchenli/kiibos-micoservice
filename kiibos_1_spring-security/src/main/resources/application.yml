spring:
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML5
  mvc:
    date-format: yyyy-MM-dd HH:mm:ss
  jackson:
    joda-date-time-format: yyyy-MM-dd HH:mm:ss
    date-format: yyyy-MM-dd HH:mm:ss
  datasource:
    url: jdbc:mysql://localhost:3306/kiibos_micoservice_spring_security?characterEncoding=utf8&useSSL=false
    username: root
    password: cl1234567
    driver-class-name: com.mysql.jdbc.Driver
    filters: stat,wall
